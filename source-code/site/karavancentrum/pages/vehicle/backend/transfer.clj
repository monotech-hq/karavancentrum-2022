
(ns site.karavancentrum.pages.vehicle.backend.transfer
  (:require
    [x.server-core.api :as a]
    [server-fruits.io  :as io]
    [mongo-db.api      :as mongo-db]))

;; -----------------------------------------------------------------------------
;; ---- Configurations ----

;; ---- Configurations ----
;; -----------------------------------------------------------------------------
